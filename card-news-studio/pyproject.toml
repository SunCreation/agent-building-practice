[project]
name = "card-news-studio"
version = "0.1.0"
description = "A local card news agent tutorial with resumable SDK and CLI engines"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.34,<1", "playwright>=1.50,<2", "claude-agent-sdk>=0.1,<1"]
[dependency-groups]
dev = ["httpx>=0.28,<1"]
[tool.uv]
package = false
