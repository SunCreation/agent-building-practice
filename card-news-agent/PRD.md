# 사용자와 함께 만드는 카드뉴스 에이전트

## 제품의 목적
주제 하나를 입력하면 최신 자료를 조사하고, 사용자가 고른 정보를 바탕으로 독자와 전달 방식을 판단하여 카드뉴스를 만든다. 사용자는 브라우저에서 중요한 선택을 하고 결과를 검토한다. AI가 단순히 원고를 한 번 생성하는 서비스가 아니라, 조사 결과와 사용자 답변에 따라 다음 행동을 바꾸는 워크플로다.

## 구현을 시작하는 AI에게
먼저 이 문서 전체를 읽고 아래 미정 사항을 사용자에게 2~3개씩 짧게 질문한다. 이미 답한 것은 다시 묻지 않는다. 추천과 이유를 함께 제시하되 사용자가 답하기 전에 결정하지 않는다. 답을 DECISIONS.md에 기록하고 작은 실행 단위로 구현한다. API 키나 인증 파일의 내용을 대화·로그·저장소에 복사하지 않는다.

- 구현 언어: Python / TypeScript(Node.js) / Rust / 다른 언어 중 무엇을 사용할지.
- 첫 실행 엔진: Claude Agent SDK / Claude CLI / OpenCode CLI 중 하나. 전부 구현하는 것은 필수 아님.
- 사용할 검색 수단과 해당 계정에서 실제 사용할 수 있는지.
- 기본 카드 비율과 결과 보관 위치. 포트 기본값은 8765, 충돌 시 다른 포트를 안내한다.
- 남은 구현상의 선택은 합리적인 기본안을 제안하고 기록한다.

언어 선택을 제한하지 않는다. 다만 선택한 SDK의 지원 언어와 구현 언어가 다르면 CLI 또는 별도 작업 프로세스로 연결하는 비용을 설명한다. 이미지 생성 작업자는 Antigravity CLI(agy)를 사용한다. 설치·로그인·필요 권한을 확인하고 이용 불가 시 성공한 척하지 않는다.

## 사용자 경험과 수용 조건
### 1. 주제 입력과 넓은 조사
웹 앱은 기본적으로 127.0.0.1에만 바인딩한다. 사용자가 주제를 입력한다. 예시 입력은 `AI 최신소식`. 이 경우 기본 조사 기간은 실행 시점 기준 최근 7일이며 화면에 조사 기준 시각과 기간을 표시한다. 결과가 부족하면 30일로 넓힐지 묻는다. 과거에 준비한 예시를 실제 최신 검색 결과로 표시하지 않는다.

가능하면 서로 다른 7~12개 후보를 조사한다. 같은 발표를 다룬 기사는 묶고, 모델·제품·연구·개발 도구 등 주제에 맞게 다양성을 확보한다. 후보마다 제목, 짧은 요약, 게시일, 사건 발생일(확인되는 경우), 원문 링크, 선택할 가치와 불확실성을 표시한다. 날짜를 확인하지 못하면 미확인으로 표시한다. 검색 수단이 없거나 호출이 실패하면 원인을 보여 주고 재시도하도록 한다.

### 2. 브라우저에서 정보 선택
후보를 체크해 1~3개 선택하고 다음 단계로 진행한다. 선택 내용은 서버에 저장한다. 선택 전에는 최종 제작을 시작하지 않는다. 새로고침해도 후보·선택·진행 상태가 유지된다.

### 3. 심층 조사와 편집 판단
선택한 후보의 원문을 확인하고 가능하면 발표 기관·논문·공식 문서를 우선 사용한다. 사실, 출처, 한계, 상충하는 내용을 구분한 조사 기록을 만든다. 기사 제목만으로 사실을 확정하지 않는다. 공개 예정인 것을 출시된 것으로, 업체 주장을 독립 검증된 사실로 바꾸지 않는다.

AI는 조사 결과로 독자, 독자에게 유용한 이유, 카드 형식, 후킹 문구 2~3개를 제안한다. 기존 입력으로 충분히 판단할 수 있으면 추천 이유를 제시하며 진행한다. 대상 독자나 편집 방향에 따라 결과가 크게 달라지면 웹에서 객관식 질문을 보여 준다. 예: `어떤 독자에게 전달할까요?` → 일반 사용자 / 개발자 / 교육자. 각 선택의 차이를 한 줄로 설명하고 자유 입력도 허용한다. 모든 단계를 질문으로 만들지는 않는다. 후킹 문구는 확인된 사실의 범위 안에서 작성한다.

후킹을 전체 원고 생성 요청의 한 문장으로 처리하지 않는다. **후킹 설계 호출 → 카드 작성 호출**로 분리한다. 설계 단계는 서로 다른 각도 두 개를 독자 관심, 궁금증, 사실 근거, 뒤 카드에서 해소할 내용, 과장 위험으로 비교하고 하나를 선정한다. 카드 작성 단계는 선정 문구를 첫 카드 제목에 그대로 반영한다. 초기 기획과 텍스트 피드백 재기획 모두 이 단계를 거친다. 문구 일치 검사는 반영 여부를 확인하는 장치이며 실제 독자 반응을 보장하지 않는다.

### 4. 스토리보드 승인
보통 5~8장으로 기획하되 내용에 맞게 조정한다. 각 카드에 역할, 제목, 핵심 문장, 근거 출처, 그림 계획을 기록한다. 표지는 관심을 끌고, 본문은 하나의 핵심을 전달하며, 마지막 카드는 요약 또는 다음 행동을 안내한다. 근거 없는 순위·수치·과장을 금지한다. 사용자는 전체 기획을 승인하거나 텍스트 피드백으로 변경을 요청한다. 수정 요청 중 제작 승인과 중복 요청을 막는다. 기존 기획은 새 결과 검증이 끝날 때까지 보존하고, 성공하면 수정 전 기획과 피드백을 이력으로 저장한다. 수정 완료 후 다시 승인 대기 상태로 돌아오며 자동으로 이미지를 만들지 않는다. 참고 구현의 재기획 API는 POST /api/projects/{id}/plan-revisions이며 instruction을 받는다.

### 5. 이미지와 글자를 분리해 제작
이미지 생성은 Antigravity CLI의 내장 이미지 도구를 자연어로 요청한다. `agy image generate` 같은 확인하지 않은 명령을 만들지 않는다. agy 응답의 JSON 형식과 PNG 이미지 형식을 혼동하지 않는다. 생성 작업 후 실제 파일 존재, 이미지 형식, 크기를 검사한다. 파일 생성이 실패하면 해당 카드에 실패와 재시도 버튼을 표시한다.

생성 요청에는 구도·분위기·색·글자를 배치할 빈 공간을 포함한다. 그림에 최종 문구를 그려 넣게 하지 않고, HTML/CSS·Canvas·SVG·Pillow 등 선택한 렌더러에서 글자를 별도로 배치한다. 한국어 글꼴, 줄바꿈, 잘림, 대비와 휴대폰 크기에서의 가독성을 확인한다. 기본 결과 크기는 1080×1350 또는 1080×1080 중 선택한다. 출력 파일에 실제 적용됐는지 검사한다.

관련 이미지가 필요한 경우 CC0·공개 도메인 또는 이용 조건에 맞는 스톡 사진을 사용할 수 있다. 인터넷 검색 결과라는 이유만으로 사용하지 않는다. 출처 URL·작가·라이선스·필요 표기를 기록한다. 생성 이미지에는 생성 도구·날짜·프롬프트를 기록한다. 생성 이미지를 무조건 저작권 없는 이미지로 부르지 않는다. 실제 제품 화면이나 실제 사건의 사진으로 오해할 수 있으면 생성 이미지임을 밝힌다. 디자인 레퍼런스는 정보 배치 원리를 참고하며 이미지나 디자인을 그대로 복제하지 않는다.

### 6. 검수, 부분 수정, 내려받기
브라우저에서 카드 전체와 개별 카드를 본다. 사용자는 특정 카드를 골라 수정 요청한다. 수정 범위 밖에서 승인된 카드가 불필요하게 바뀌지 않도록 버전을 보관한다. 사실/출처 점검과 글자 잘림/가독성 점검을 별도로 수행한다. 최종 승인 후 카드별 PNG, 전체 ZIP, 출처·이미지 사용 기록을 제공한다. PDF는 선택 기능이다.

## 실행 구조
웹 서버는 화면과 영속 상태를 관리하고, 작업 프로세스가 AI 호출과 이미지 생성을 수행한다. 긴 HTTP 요청 하나를 열어 둔 채 모든 작업을 완료하지 않는다. 작업 ID를 즉시 반환하고 폴링 또는 SSE로 진행 상황을 전달한다. 명령 실행은 인자 배열을 사용하며 사용자가 입력한 주제를 셸 명령 문자열에 이어 붙이지 않는다. 작업별 디렉터리를 분리한다.

공통 엔진 인터페이스는 시작, 답변 전달, 상태 조회, 취소를 제공한다. 내부 결과는 progress / need_input / result / error로 정규화한다. 이는 이 앱에서 정하는 규약이며 모든 CLI에 내장된 공통 규격이 아니다. SDK 질문 이벤트 또는 CLI의 구조화된 요청을 앱의 질문으로 변환한다.

프로젝트 ID, 작업 ID, 실행 엔진, 엔진 세션 ID, 상태, 현재 질문, 선택 답변, 결과 버전, 오류를 저장한다. `waiting_for_user`는 실패가 아니다. 질문에는 ID·버전·질문·선택지·추천 이유를 둔다. 답변은 질문 ID와 버전을 확인해 한 번만 반영하고 오래된 화면의 답변은 거절한다. 추천 선택지가 보이더라도 사용자 응답으로 간주하지 않는다.

CLI 방식은 `need_input`을 반환하고 현재 호출을 끝낸 뒤, 웹 답변을 같은 엔진 세션에 다음 요청으로 전달해 이어갈 수 있다. SDK의 대기 콜백은 웹 요청과 분리한 작업에서 관리한다. 서버 재시작 시 중단 상태를 인식하고 저장한 세션과 작업 기록을 이용해 복구하거나 명확한 재시도 선택을 보여 준다. 엔진이 바뀌면 세션 ID를 재사용하지 않고 저장된 프로젝트 요약으로 새 세션을 연다. 서로 다른 프로젝트에 '가장 최근 세션 이어가기'를 사용하지 않는다.

## 웹 API 계약 초안
아래는 필수 의미를 담은 초안이다. 구현 언어에 맞춰 상세 타입과 오류 응답을 API_SPEC.md에 확정한다.

| 요청 | 입력 | 결과 |
|---|---|---|
| POST /api/projects | topic, optional period | projectId |
| POST /api/projects/{id}/research | requestId | runId, status |
| GET /api/projects/{id} | 없음 | 후보·선택·상태·질문·산출물 |
| POST /api/projects/{id}/selection | candidateIds, version | 저장된 선택 |
| POST /api/projects/{id}/answers | questionId, version, answer, requestId | 수락 여부·현재 상태 |
| POST /api/projects/{id}/approve | stage(storyboard/final), version, requestId | 제작 작업 ID 또는 최종 승인 상태 |
| POST /api/projects/{id}/revisions | cardIds, instruction, version, requestId | 수정 작업 ID |
| POST /api/runs/{id}/cancel | 없음 | 취소 상태 |
| GET /api/projects/{id}/exports | 없음 | 검증된 다운로드 목록 |

변경을 일으키는 요청은 프로젝트 범위에서 requestId로 중복을 식별한다. 같은 ID·같은 입력은 저장한 결과를 반환하고 같은 ID·다른 입력은 충돌로 거절한다. 진행 작업 중 같은 요청을 반복해도 중복 제작하지 않는다. 최종 승인은 현재 결과 버전을 대상으로 저장하고, 수정 시 승인을 해제한다. exports는 최종 승인된 버전만 제공한다. 인증 필요·검색 실패·이미지 실패·잘못된 답변·취소를 구분한다. 오류 화면은 이유와 가능한 다음 행동을 제시한다. 취소는 실행 중인 하위 프로세스까지 정리한다.

## 구현과 확인 순서
1. 질문을 통해 미정 사항을 결정하고 API 계약과 화면 흐름을 작성한다.
2. 브라우저의 주제 입력·후보 선택·질문 대기·답변 저장을 먼저 만든다. 모의 데이터는 화면에 명시한다.
3. 선택한 엔진으로 실제 조사와 세션 이어가기를 연결한다.
4. 실제 Antigravity 이미지 한 장의 생성과 파일 검증을 먼저 통과시킨다.
5. 스토리보드, 글자 렌더링, 부분 수정, 내보내기를 연결한다.
6. ACCEPTANCE.md로 전체 흐름과 실패 복구를 확인한다. 미검증 항목을 성공으로 적지 않는다.

완료 산출물은 실행 가능한 구현, 쉬운 README, 결정 기록, API 명세, 실제 실행 결과와 알려진 한계다. 이 PRD 파일 자체는 완성 앱이 아니다.
