[project]
name = "card-news-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.34,<1", "playwright>=1.50,<2", "pillow>=11,<13"]
[dependency-groups]
dev = ["httpx>=0.28,<1", "pytest>=8,<10"]
[tool.uv]
package = false
